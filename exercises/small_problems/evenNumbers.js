// LS: Log all even numbers from 1 to 99,...
// inclusive, to the console, with each number on a separate line

// Log all the numbers including 1 and 99...
// to the console if they are even (but they aren't even so we won't log them)
// Make sure each number is on a different line as they get logged

// start
// Set iterator;
// while (iterator < number randge)
// log iterator
// iterator += 1;
// end


let index = 2;

while (index < 99) {
  console.log(index);
  index += 2;
}
